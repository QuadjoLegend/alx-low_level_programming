#include "main.h"

/**
 * _abs - check code
 *
 * @i: An integer input
 *
 * Descripton: The function prints and absolute value of a number
 *
 * Return: Absolute value of a number
 */

int _abs(int i)
{
	if (i < 0)
	{
		return (-i);
	}
	else
	{
		return (i);
	}
}
