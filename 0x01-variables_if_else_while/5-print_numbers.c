#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * Main - Entry
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int n;

	if (n == 0 && n <= 9)
		printf("%d", n);
	printf('\n');

	return (0);
}
